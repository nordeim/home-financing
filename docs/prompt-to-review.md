Please carefully read completely and internalized your operating instructions in the attached/pasted text.
Now, please `git clone https://github.com/nordeim/home-financing.git` to your local workspace, then meticulously review the included `AGENTS.md` , `CLAUDE.md` , `README.md` and `Project_Architecture_Document.md` to have a good understanding of the purpose of the project and its current codebase design and architecture. Next, meticulously plan to validate your deep understanding against the codebase to check for alignment to confirm the current project status. Then meticulously plan to update  `AGENTS.md` , `CLAUDE.md` , `README.md` and `home-financing_SKILL.md` to align them with the current repo codebase.

The repo included `skills/` and `infrastructure/` folders are to be excluded from code checking, testing and compilation.

The codebase has been deployed to live website at URL `https://modfii.jesspete.shop/`. Please meticulously plan to run browser-based E2E tests to identify gaps and bugs/issues. The repo codebase is meant to be a fully working/functioning, production-ready and enterprise clone of `https://modfii.com/`, so compare the visual aesthetics and UI/UX design and animations of `https://modfii.jesspete.shop/` with the source `https://modfii.com/`, try to make the former website (working clone) look as close to the latter (source) as possible visually.

Next, meticulously plan to create a comprehensive remediation plan with a detailed ToDo list to fix the identified codebase issues, bugs and gaps. Next, review and validate the remediation plan against the codebase again to ensure alignment before executing it meticulously. Use TDD approach to make code changes. Look for appropriate skills in the repo included `skills/` folder to help you in the planning (refer to the included `skills/skills-catalog.md` to look for suitable skills)

Next, update the relevant documentation to ensure alignment with the remediated codebase.

Finally, `git commit` all code and document changes locally and then `git push` them to my GitHub repo `git@github.com:nordeim/home-financing.git` using the attached ssh key `ssh-key.txt` and wrapper script `skills/how-to-git-push-using-ssh-wrapper/scripts/ssh_git_wrapper_v3.py`.

Do not create any new git branch. All git commits must be to the main branch.
